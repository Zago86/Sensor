<Controls:MetroWindow x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="72*"/>
            <RowDefinition Height="72*"/>
            <RowDefinition Height="185*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="122*"/>
            <ColumnDefinition Width="153*"/>
            <ColumnDefinition Width="242*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="BT_OpenClient" Content="Open Client" Margin="0" Style="{DynamicResource SquareButtonStyle}" Click="BT_OpenClient_Click" />
        <Button x:Name="BT_CloseClient" Content="Close Client" Margin="0" Grid.Row="1" Style="{DynamicResource SquareButtonStyle}" Click="BT_CloseClient_Click"/>
        <TextBox x:Name="IpAddress" Grid.Column="1" Margin="0" TextWrapping="Wrap" Text="127.0.0.1" FontWeight="Bold" TextAlignment="Center"/>
    </Grid>
</Controls:MetroWindow>
